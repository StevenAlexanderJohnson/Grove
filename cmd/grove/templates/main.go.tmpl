package main

import (
	"net/http"
	"github.com/StevenAlexanderJohnson/grove"
)

func main() {
	scope := grove.
		NewScope("main").
		WithRoute("/", http.HandlerFunc(
			func(w http.ResponseWriter, r *http.Request) {
				w.Write([]byte("Hello, {{.ProjectName}}!"))
			},
		))

	app := grove.NewApp("{{.ProjectName}}").WithScope("/", scope)

	if err := app.Run(); err != nil {
		panic(err)
	}
}
